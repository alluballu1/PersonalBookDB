export const fetchData = () => {
  return async (dispatch) =>{
    const data = "fdsa"
  }
}

const bookReducer = (state = [], action) => {
  switch (action) {
    default:
      return state;
  }
};

export default bookReducer;